import LogoutButton from "@/features/auth/components/LogoutButton";
import { Text, View } from "react-native";

export default () => {
  return (
    <View>
      <Text>Dashboard</Text>
      <LogoutButton />
    </View>
  );
};
